version: '2'

services: 

  nodejs:
    build: .
    volumes: 
      - ./src:/api/src
      - ./config:/api/config
    env_file:
      - var.env
    ports:
      - "3000:3000"

  nginx:
    image: prayforhana/test3:0.1
    links:
      - nodejs:nodejs
    ports:
      - "80:80"
      